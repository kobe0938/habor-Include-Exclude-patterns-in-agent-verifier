job_name: e2e-cfg-baseline
jobs_dir: tmp-e2e/jobs
n_attempts: 1
n_concurrent_trials: 1
environment:
  type: daytona
  delete: true
agents:
  - name: oracle
tasks:
  - path: tmp-e2e/tasks/log-filter-demo
